function navBar() {
  const nav = document.selectById("myTopNav");
  if (nav === ".topNav") {
    nav.className += " responsive";
  } else {
    nav.className = "topnav";
  }
}
nav.onclick === true;
navBar();
